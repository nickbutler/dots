# Epic: <Name>

## Summary

<One paragraph: what this epic delivers and to whom. Avoid restating individual features — this is the elevator pitch.>

## Scope

**In scope:**
- <Concrete, behaviour-level statements>
- <...>

**Out of scope:**
- <Things a reader might assume are included but aren't>
- <Adjacent areas that belong to other epics, with pointer to which>

## Key concepts

<Domain terms specific to this epic that appear across features. 3-8 items. One line each.>

- **<Term>**: <Definition in the context of this system.>
- **<Term>**: <...>

## Architecture notes

<Where this epic lives in the codebase, which modules it spans, and which external dependencies it touches. Reference paths, do not duplicate content from ARCHITECTURE.md — link to it where relevant.>

- **Implementation locations**: `<path/to/module>`, `<path/to/module>`
- **External dependencies**: <APIs, services, queues>
- **Cross-epic touchpoints**: <Other epics this collaborates with>

## Features

- [<Feature name>](features/<feature-slug>.md) — <one-line description>
- [<Feature name>](features/<feature-slug>.md) — <one-line description>

## Dependencies and integration points

<Other epics this depends on, or that depend on this. External systems involved. Anything that matters at the boundary.>

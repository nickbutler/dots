# Architecture

<One or two sentences: what this system is and what problem it solves. No jargon.>

This document describes the current technical design. The reasoning behind structural choices lives in DESIGN_DECISIONS.md — update both when the architecture changes.

## Design principles

<The "how we build" rules that apply across all features. Each principle should be actionable: a developer reading it should be able to apply it when making a decision. Include the rationale briefly — a principle without a reason is a rule without teeth.>

- **<Principle name>**: <What it means in practice. Why it matters for this project.>
- **<Principle name>**: <...>

## Constraints

<Hard limits that are non-negotiable — technical, legal, operational, or organisational. These are not preferences; they are boundaries that features must be designed within.>

- **<Constraint>**: <What is ruled out or required, and why it exists.>
- **<Constraint>**: <...>

## Module structure

<How the codebase is layered or partitioned. Reference actual paths. Describe the purpose of each layer and the rule for what belongs there.>

- **`<path/to/layer>`**: <What lives here and what belongs here vs elsewhere.>
- **`<path/to/layer>`**: <...>

## Key abstractions

<The core types, interfaces, or contracts the system is built around. What a new developer must understand before anything else makes sense.>

- **`<TypeName / InterfaceName>`**: <What it represents and why it is a central abstraction.>
- **`<TypeName / InterfaceName>`**: <...>

## Cross-cutting concerns

<Behaviour that spans multiple features: auth, error handling, logging, caching, background jobs, etc. Where each is implemented and any invariants that apply system-wide.>

- **<Concern>**: <How it is handled and where.>

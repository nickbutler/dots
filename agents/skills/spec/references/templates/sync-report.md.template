# Sync report

Generated by spec-sync on <date>.

This report is informational. Actual changes have been applied directly to spec files — review via `git diff specs/` before committing.

## Detection

- **Method**: <git-diff from `<sha>` to HEAD | mtime fallback>
- **Range**: <commits or time range covered>
- **Source files inspected**: <count>
- **Spec files inspected**: <count>

## Summary

| Category | Count |
|---|---|
| Features unchanged | <n> |
| Features updated (drift) | <n> |
| Features updated (refactor only) | <n> |
| Features archived (deprecated) | <n> |
| Features added (uncovered code) | <n> |
| Gaps resolved | <n> |
| Gaps still open | <n> |
| Gaps newly added | <n> |
| @draft scenarios cleared (steps implemented) | <n> |
| @draft scenarios still pending | <n> |
| @draft scenarios newly added | <n> |

## Changes by feature

### Drift updates

- **`features/<feature-slug>.md`**: <one-line summary of what changed and why>

### Refactor-only updates

- **`features/<feature-slug>.md`**: <e.g. "updated implementation path from X to Y after module move; scenarios unchanged">

### Archived features

- **`archived/<feature-slug>.md`** (was at `features/<feature-slug>.md`): <reason; last commit where feature existed>

### Newly added features

- **`features/<feature-slug>.md`**: <one-line description; source files>

## Gap reconciliation

### Resolved

- **<Feature or area>**: <Original question, brief note on how it was resolved, where it now lives in specs.>

### Newly added

- **<Feature or area>**: <Brief note on the new gap and why it matters.>

## @draft scenario reconciliation

### Ready (tag removed — steps now implemented)

- `<Scenario name>` in `features/<feature-slug>.feature`

### Still pending (steps not yet implemented)

- `<Scenario name>` in `features/<feature-slug>.feature` — <step phrase(s) needed>

### Newly tagged

- `<Scenario name>` in `features/<feature-slug>.feature` — <step phrase(s) needed>

## Clarifications requested

| Question | Resolution |
|---|---|
| <Specific question raised during sync> | <How the user resolved it> |

## Patterns and observations

<Things noticed during the sync that may be worth a separate discussion. Do not fix during sync. Examples:>

- <Inconsistent naming between two features that probably should align>
- <A code area where coverage is consistently thin>
- <Step definitions that look like they could be consolidated>

## Next steps

- Review changes with `git diff specs/`
- Commit if satisfied, or amend specific files before committing
- If any items were deferred during clarifications, they're noted above for follow-up

# Sync report

Generated by spec-sync on <date>.

This report is informational. Actual changes have been applied directly to spec files — review via `git diff specs/` before committing.

## Detection

- **Method**: <git-diff from `<sha>` to HEAD | sync-marker from `<sha>` | mtime fallback>
- **Range**: <commits or time range covered>
- **Source files inspected**: <count>
- **Spec files inspected**: <count>

## Summary

| Category | Count |
|---|---|
| Features unchanged | <n> |
| Features updated (drift) | <n> |
| Features updated (refactor only) | <n> |
| Features archived (deprecated) | <n> |
| Features added (uncovered code) | <n> |
| Gaps resolved | <n> |
| Gaps still open | <n> |
| Gaps newly added | <n> |
| Step proposals satisfied | <n> |
| Step proposals still pending | <n> |
| Step proposals newly added | <n> |

## Changes by feature

### Drift updates

- **`<epic-slug>/features/<feature-slug>.md`**: <one-line summary of what changed and why>
- **`<epic-slug>/features/<feature-slug>.md>`**: <...>

### Refactor-only updates

- **`<epic-slug>/features/<feature-slug>.md`**: <e.g. "updated implementation path from X to Y after module move; scenarios unchanged">

### Archived features

- **`<epic-slug>/archived/<feature-slug>.md`** (was at `<epic-slug>/features/<feature-slug>.md`): <reason; last commit where feature existed>

### Newly added features

- **`<epic-slug>/features/<feature-slug>.md>`**: <one-line description; source files>

## Gap reconciliation

### Resolved

- **<Epic name> / <Feature or area>**: <Original question, brief note on how it was resolved, where it now lives in specs.>

### Newly added

- **<Epic name> / <Feature or area>**: <Brief note on the new gap and why it matters.>

## Step definition reconciliation

### Satisfied (removed from proposals)

- `<Step phrase>` — now implemented in `<path/to/step_definition_file>`

### Newly proposed

- `<Step phrase>` — needed for `<feature>`; rationale in STEP_DEFINITIONS_PROPOSED.md

## Clarifications requested

| Question | Resolution |
|---|---|
| <Specific question raised during sync> | <How the user resolved it> |
| <...> | <...> |

## Patterns and observations

<Things noticed during the sync that may be worth a separate discussion. Do not fix during sync. Examples:>

- <Inconsistent naming between two features that probably should align>
- <An epic that has grown enough to warrant splitting>
- <A code area where coverage is consistently thin>
- <Step definitions that look like they could be consolidated>

## Next steps

- Review changes with `git diff specs/`
- Commit if satisfied, or amend specific files before committing
- If any items were deferred during clarifications, they're noted above for follow-up
